# Source materials — local-only, never committed
# (original client assets: .jpeg photos, .mp4 video, .docx specs, reference HTML)
user-docs/

# Raw video files anywhere in the tree (CDN-only)
*.mp4

# Claude Code internal state
.claude/
CLAUDE.md

# Playwright / tool residue
.playwright-mcp/

# Screenshots at repo root (working artifacts — mockups/*/screenshots/ stay)
/*.png

# Placeholder files
**/.gitkeep
